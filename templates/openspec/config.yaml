schema: spec-driven

context: |
  # Filled by openflow init / project — describe stack and product language here.

rules:
  specs:
    - Prefer user-facing product behavior and observable outcomes
  tasks:
    - Keep tasks checkbox-driven and incremental
